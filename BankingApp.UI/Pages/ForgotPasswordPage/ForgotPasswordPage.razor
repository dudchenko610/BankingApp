@page "/forgotPassword"
@attribute [Unauthorized]

<div class="container shadow p-3 mb-5 bg-white rounded w-50 mt-3">
    <EditForm Model="@_forgotPasswordView" OnValidSubmit="async () => { await OnFormSubmitAsync(); }">
        <DataAnnotationsValidator />

        <div class="mb-3">
            <label for="email" class="form-label">Email</label>
            <InputText @bind-Value="_forgotPasswordView.Email"
                       id="email"
                       placeholder="email"
                       class="form-control" />
            <ValidationMessage For="@(() => _forgotPasswordView.Email)" />
        </div>
        <button type="submit" disabled="@(!context.Validate() || !context.IsModified())" class="btn btn-primary btn-block">Reset Password</button>
    </EditForm>
</div>
